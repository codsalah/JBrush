import UIFrame.PainterFrame;
import javax.swing.SwingUtilities;

public class Main {
        public static void main(String[] args) {
        // Ensure UI updates match the professional feel
        SwingUtilities.invokeLater(() -> new PainterFrame());
    }
}
